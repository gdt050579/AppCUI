add_subdirectory(Factory)
add_subdirectory(TestTerminal)
if (MSVC)
    add_subdirectory(WindowsTerminal)
else()  
    if (APPCUI_HAS_NCURSES)
        add_subdirectory(NcursesTerminal)
    endif()
    if (APPCUI_HAS_SDL)
        add_subdirectory(SDLTerminal)
    endif()
endif()